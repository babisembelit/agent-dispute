[build]
  # Run build from the root to ensure SDK dependency is handled correctly
  command = "cd sdk && npm install && npm run build && cd ../frontend && npm install && npm run build"
  
  # Point to the distribution folder relative to the root
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"
